# CapitalScope Terminal — Environment Variables
# Copy this file to .env.local and fill in your keys.

# Anthropic Claude API key — required for:
#   Gold Mining Scanner, Deep Dive Research,
#   Market Research Agent, Model Builder
VITE_ANTHROPIC_API_KEY=sk-ant-your-key-here

# Backend must run on port 8000 (Vite proxy is pre-configured):
#   uvicorn main:app --reload --port 8000
